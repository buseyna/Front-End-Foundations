//string
let name = 'Brittany';

//number
let age = 33;

//boolean
let married = false;

//array
let dogs = ['Pepper', 'Spot', 'Tank', 'Winston'];

//object - Potential Values
let house = {
  numberOfBedrooms: 4,
  numberOfBathrooms: 2.5,
  hasPool: false,
  location: 'San Francisco',
  price: '1.3 million'
};

//Null
let house1 = {
    numberOfBedrooms: 4,
    numberOfBathrooms: 2.5,
    hasPool: null,
    location: 'San Francisco',
    price: '1.3 million'
  };
  console.log(house.hasPool);

  //Undefined
  let newHouse;
console.log(newHouse);

//Deaclering a avariable
let newString = "bubu";

//Deaclering a avariable assign it any numeric value.
let newNumber = 7;

//Declare a new variable named newBoolean and assign it any boolean value.
let newBoolean = true;

//const newBoolean = true;
//preview : I can see that you defined the variable 'newBoolean'. However, I was unable to change its value, which means it's a constant. The 'let' keyword allows the variable to be changed.

